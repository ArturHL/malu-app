export default class Address {
  constructor(id, name, image) {
    this.id = id;
    this.name = name;
    this.image = image;
  }

  // En las clases deberia estar el metodo para transformar la data de la API en objetos de la clase
}